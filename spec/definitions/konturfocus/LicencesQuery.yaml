type: object
allOf:
  - $ref: '#/definitions/KonturFocusRequest'
  - type: object
    required:
      - inn
    properties:
      inn:
        type: array
        items:
          type: string
      ogrn:
        type: array
        items:
          type: string

